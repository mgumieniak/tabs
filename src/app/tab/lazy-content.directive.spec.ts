import { LazyContentDirective } from './lazy-content.directive';

describe('LazyContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyContentDirective();
    expect(directive).toBeTruthy();
  });
});
