import { TabIndicatorDirective } from './tab-indicator.directive';

describe('TabIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TabIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
